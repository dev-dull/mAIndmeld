apiVersion: v1
kind: Secret
metadata:
  name: maindmeld-env
type: Opaque
stringData:
  # Values here are placeholders. Replace them, or create the Secret out of
  # band and delete this file from the kustomization.
  GEMINI_API_KEY: "replace-me"
  # Uncomment for the notifier you configure in configmap.yaml.
  # NOTIFY_WEBHOOK_SECRET: "replace-me"
  # NTFY_TOKEN: "replace-me"
