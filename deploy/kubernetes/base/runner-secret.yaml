apiVersion: v1
kind: Secret
metadata:
  name: maindmeld-runner-env
type: Opaque
stringData:
  # Placeholders. Replace them, or create the Secret out of band (External
  # Secrets, sealed secrets) and delete this file from the kustomization.
  MAINDMELD_RUNNER_TOKEN: "replace-me"
  ANTHROPIC_API_KEY: "replace-me"
